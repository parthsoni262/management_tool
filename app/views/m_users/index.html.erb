<table>
  <thead>
    <th>Id</th>
    <th>User Id </th>
    <th>Mail Id</th>
    <th>Psssword</th>
    <th>M Company Id</th>
  </thead>
  <tbody>
    <% @muser.each do |c| %>
      <tr>
        <td><%= c.id %></td>
        <td><%= c.user_id %></td>
        <td><%= c.mail_id %></td>
        <td><%= c.password%></td>
        <td><%= c.m_company%></td>
        <td><%= link_to 'Edit', edit_m_user_path(@muser) %></td>
        <td><%#= link_to 'Delete', @muser, method: :delete, data: {confirm: 'Are you sure you want to delete this '} %></td>
      </tr>
    <% end %>
  </tbody>
</table>